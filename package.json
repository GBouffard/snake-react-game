{
  "name": "snake-react-game",
  "version": "0.1.0",
  "homepage": "https://gbouffard.github.io/snake-react-game/",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "2.1.8",
    "styled-components": "^4.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "postinstall": "touch .env && echo \"SKIP_PREFLIGHT_CHECK=true\" > .env",
    "test": "react-scripts test",
    "build": "react-scripts build",
    "deploy": "npm run build && gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "is-touch-device": "^1.0.1",
    "standard": "^12.0.1"
  }
}
